package gui;

import model.AppState;
import javax.swing.*;
import java.awt.*;

public class Step2DefinePanel extends JPanel {
    public Step2DefinePanel(AppState appState, MainFrame mainFrame) {
        setBackground(new Color(20, 20, 20));
        add(new JLabel("Step 2 - Define (coming soon)"));
    }
    public void onShow() {}
}