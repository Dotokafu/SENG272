package gui;

import model.AppState;
import javax.swing.*;
import java.awt.*;

public class Step3PlanPanel extends JPanel {
    public Step3PlanPanel(AppState appState, MainFrame mainFrame) {
        setBackground(new Color(20, 20, 20));
        add(new JLabel("Step 3 - Plan (coming soon)"));
    }
    public void onShow() {}
}